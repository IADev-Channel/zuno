{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm -w @iadev/zuno run build && npm -w @iadev/zuno-react run build && npm -w @iadev/zuno-express run build"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "cors": "^2.8.5",
    "typescript": "^5.9.3"
  }
}